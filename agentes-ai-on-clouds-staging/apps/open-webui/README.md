# Open WebUI

En legacy corre como contenedor `open-webui` (`ghcr.io/open-webui/open-webui:main`).

Para cloud: desplegar Helm chart oficial o manifest propio apuntando a LiteLLM como backend OpenAI-compatible.

No se copia estado de usuarios desde legacy; configurar en el primer deploy.
